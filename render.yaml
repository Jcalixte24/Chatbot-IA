services:
  - type: web
    name: chatbot-ia-institut
    runtime: python
    buildCommand: pip install -r requirements.txt && python scraper.py && python build_index.py
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
